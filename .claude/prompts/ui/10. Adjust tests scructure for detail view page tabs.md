#For reviewers: tests for tabs were generated with command so no specific prompt used for that

create folder inside tests/ui/parts named /detail-view-tabs. Split                                 
@ui/tests/ui/parts/part-detail-tabs.spec.ts to different test file so that we have 1 file per tab.   
Place these files inside new folder  