/create-page-object https://demo.inventree.org/web/part/82/details PartsDetailViewPage. Note: 82 in URL is product id, so can be different. For now I want you to create 
1. Header locators to verify Part name/id, description, In stock count
 2. navigation bar - create component for that. It should have methods like chooseTab("Part Details" | "Stock" | ...) so that we can navigate between tabs in tests
 3. Empty page components for each tab (all extends from BaseComponent because it is part of the page)