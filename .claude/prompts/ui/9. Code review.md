Address code review issues:

TC-UI-PC-003:117 — Direct page usage breaks Page Object abstraction; add selectCategory() to CreatePartModal
TC-UI-PC-006:298 — Validation only checks error banner exists, not specific field error message
test.beforeEach:8 — 120s timeout applied to all tests; move to specific import tests only
TC-UI-PC-007:377 — No verification import succeeded or modal closed
TC-UI-PC-008:413 — Test incomplete; missing error indicator and correction workflow verification