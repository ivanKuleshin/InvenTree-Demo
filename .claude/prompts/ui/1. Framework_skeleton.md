You are a senior QA Automation Architect.

Your task is to DESIGN a scalable, production-ready Playwright + TypeScript test automation framework.

---

## Context

Target application:
https://demo.inventree.org/

Application type:
- Web application (inventory / ERP-like system)
- Role-based access control (RBAC)

Authentication:
- DO NOT implement user registration
- Use predefined test users with known credentials
- Multiple users with different roles and permissions

Example roles:
Username |	Description	
allaccess |	View / create / edit all pages and items	
reader |	Can view all pages but cannot create, edit or delete database records	
engineer |	Can manage parts, view stock, but no access to purchase orders or sales orders	
admin |	Superuser account, access all areas plus administrator actions

Passwords will be manually set in the .env file that must not be access by AI tools
---

## Goals

Design a framework that is:
- Scalable 
- Maintainable across multiple contributors
- Optimized for parallel execution
- Easy to debug
- Role-aware

---

---

## Step 1: Project Structure

Propose structure like:

/tests
  /ui
    /auth
    /inventory
/framework    
  /core
    ElementHolder.ts - abstract class that represents page or part of the page (component)
    BasePage.ts base class for page objects
    BaseComponent.ts class for components 
    BaseTable.ts - should extend BaseComponent and has methods that allows to work with tables and verify their content, e.g. getRowByIdx, getRowByCell etc.
  /pages
    /featureA
      Page1.ts
      Page2.ts
      Component1.ts - specific for this feature and only used in related page objects
  /components - reusable components across multiple pages, e.g. NavigationBar.ts
/fixtures
/utils 
/config
/data


---

## Step 2: Authentication & User Management

This is critical. Design:

1. User storage strategy:
   - Where credentials live (e.g. .env, JSON, secrets)
   - Structure like:

     users = {
       admin: {...},
       manager: {...},
       viewer: {...}
     }

2. Playwright fixtures for:
   - login as specific role
   - reuse authenticated sessions (storageState)

3. Session strategy:
   - one session per role
   - per test isolation vs reuse

4. Example:
   - login fixture
   - test using different roles

---

## Step 3: Core Implementation

Provide real code for:

1. playwright.config.ts
   - chromium only for now
   - parallel execution
   - retries
   - baseURL = https://demo.inventree.org/ 

2. Base fixtures
   - role-based login
   - page injection

3. Page Object example
   - e.g. LoginPage, DashboardPage
   - clean abstraction

4. Test examples:
   - same test executed for multiple roles
   - role-specific expectations

---


## Step 4: Test Data Strategy

- Avoid creating users dynamically
- Use:
  - seeded data
  - API setup (if possible)
- How to ensure tests are deterministic

---

## Step 5: Reporting & Debugging

Include:

- HTML report
- Trace viewer usage

---

## Step 6: Best Practices

Specifically address:

- DO NOT:
  - hardcode credentials in tests
  - duplicate tests per role
  - mix assertions into Page Objects

- DO:
  - centralize role logic
  - use fixtures heavily
  - keep tests business-readable

---

## Output Format

Structure response as:

1. Architecture Overview
2. RBAC Strategy
3. Folder Structure
4. Code Examples
5. CI/CD
6. Best Practices

---

## Important

- Show REAL TypeScript code (no pseudo-code)
- Optimize for maintainability, not quick hacks
- Assume multiple engineers will extend this
- Use skills: 
  - Playwright Best practices
  - 