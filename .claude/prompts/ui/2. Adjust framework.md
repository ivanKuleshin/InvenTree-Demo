You are a senior QA Automation Architect.

You previously generated a Playwright + TypeScript test automation framework.

Your task now is to REFACTOR and CORRECT the existing implementation based on strict architectural requirements below.

IMPORTANT:

- Do NOT rebuild from scratch
- Modify the existing structure and code
- Keep what is good, fix what is wrong
- Show updated code snippets (not full repo dump unless necessary)

---

## Required Changes

### 1. Fixtures Usage (STRICT RULE)

- Use Playwright fixtures ONLY for:
  - Page Object initialization

- DO NOT:
  - Put authentication logic in fixtures
  - Create role-based fixtures like `adminPage`, `userPage`, etc.

- Fixtures should be simple and predictable:
  Example:
  - provide `dashboardPage`
  - provide `loginPage`

---

### 2. Authentication Strategy (Simplify)

We are NOT testing multi-user interaction.

So:

- Use:
  test.use({ storageState: ... })

- Apply it:
  - at test.describe level OR
  - per test file

- DO NOT:
  - dynamically switch users inside fixtures
  - create complex role-based context switching

---

### 3. Storage State Management (No Duplication)

Create a centralized solution for storageState references.

Requirements:

- No hardcoded paths inside tests
- No copy-paste like:
  test.use({ storageState: 'storage/admin.json' })

Implement ONE of:

Option A (preferred):

- Enum or const object:
  e.g.
  export const STORAGE_STATE = {
  ADMIN: '...',
  MANAGER: '...',
  VIEWER: '...'
  }

Option B:

- helper function:
  getStorageState(role)

Use it in tests cleanly.

---

### 4. BasePage Improvement

Update BasePage:

- Add:
  url: string | RegExp

- Implement method:
  async waitForLoad()

Behavior:

- Wait for page to be fully loaded
- Validate current URL matches `this.url`
  - If string → exact or includes match (decide and justify)
  - If RegExp → test against it

- Throw meaningful error if mismatch

---

### 5. Login Page Object (REAL IMPLEMENTATION)

Introduce proper `LoginPage`:

- Methods:
  - navigate()
  - login(email, password)
  - waitForLoginSuccess()

- Use this page object in setup phase (storageState generation)

---

### 6. Setup Project (CRITICAL FIX)

You incorrectly used manual browser launch:

const browser = await chromium.launch();

This must be REMOVED.

Instead:

- Use Playwright "setup project" in playwright.config.ts

Requirements:

- Create a `setup` project:
  - generates storageState files for each role

- Main test project:
  - depends on setup project

- Use Playwright-native approach:
  - no manual browser lifecycle

---

### 7. Remove Fake / Mock Artifacts

Delete:

- Mock Page Objects
- Fake/demo tests not aligned with real app (InvenTree)

Keep only:

- realistic structure
- meaningful examples

---

## Expected Output

Structure your response:

1. Summary of Changes
2. Updated Architecture Decisions
3. Code Updates (only relevant parts):
   - fixtures
   - BasePage
   - LoginPage
   - storageState constants
   - playwright.config.ts (setup project)
   - example test

4. Explanation of Why This Is Better

---

## Constraints

- Use real TypeScript (no pseudo-code)
- Keep code clean and production-ready
- Avoid overengineering
- Prefer simplicity over abstraction

---

## Goal

After refactor:

- Tests are simple:
  test.use({ storageState: STORAGE_STATE.ADMIN })

- Fixtures are minimal
- Auth is stable and reusable
- Framework is easy to scale
