[
  {
    "query": "I need to understand what happens when you POST /api/part/ with a `category` that doesn't exist — does it return 400 or 404? Can you hit the demo and write a TC for it with the actual response body?",
    "should_trigger": true
  },
  {
    "query": "test the part categories api and document what fields come back on GET /api/part/category/ — I need observed response shapes before the automation team writes assertions",
    "should_trigger": true
  },
  {
    "query": "we noticed that deleting a part via DELETE /api/part/{pk}/ sometimes returns 204 and sometimes something else depending on stock. can you check a few cases and write manual TCs?",
    "should_trigger": true
  },
  {
    "query": "I need TC-APFLT test cases for filtering parts by the `active` flag combined with `category` — test GET /api/part/?active=true&category=15 and similar combinations on demo.inventree.org",
    "should_trigger": true
  },
  {
    "query": "not sure if the inventree stock endpoint paginates by default or dumps everything — can you explore /api/stock/item/ and write a few test cases documenting the pagination behavior?",
    "should_trigger": true
  },
  {
    "query": "what does a 401 vs 403 look like on the parts api? check both unauthenticated and authenticated-but-read-only requests to POST /api/part/ and write TCs showing the real response bodies",
    "should_trigger": true
  },
  {
    "query": "I'm seeing `part_count: null` on newly created categories but `part_count: 0` after a PATCH. can you confirm this behavior on the demo API and write a TC capturing the difference?",
    "should_trigger": true
  },
  {
    "query": "need manual test cases for relational integrity — specifically what happens when you PATCH /api/part/{id}/ with a `default_location` PK that doesn't exist. probe it and document the actual error",
    "should_trigger": true
  },
  {
    "query": "explore the InvenTree pricing API at /api/part/sale-price/ and /api/part/purchase-price/ — I need to understand the response shapes and write TC-APPRICE test cases from live observations",
    "should_trigger": true
  },
  {
    "query": "can you look at how the search param works on GET /api/part/?search=enclosure and write TCs for search behavior including partial match, case sensitivity, and empty result set?",
    "should_trigger": true
  },
  {
    "query": "write a REST Assured test for TC-APCRUD-003 — the one that checks GET /api/part/{id}/ with ?parameters=true returns the parameters array",
    "should_trigger": false
  },
  {
    "query": "can you review PartCrudTest.java and check whether the assertions in testGetPartWithParameters() actually match what the manual TC says we should assert?",
    "should_trigger": false
  },
  {
    "query": "look at the InvenTree docs at https://docs.inventree.org/en/stable/part/ and summarize what fields are required when creating a part",
    "should_trigger": false
  },
  {
    "query": "update TC-APVAL-002.md to add a step that verifies the DRF error format has the field name as the key and an array of strings as the value",
    "should_trigger": false
  },
  {
    "query": "I want to explore the parts creation form in the browser UI — can you navigate to demo.inventree.org and document what fields are shown and which are required?",
    "should_trigger": false
  },
  {
    "query": "generate 20 test parts via the API so we have seed data for the load test tomorrow — just create them with sequential names like TestPart-001 through TestPart-020",
    "should_trigger": false
  },
  {
    "query": "what's the difference between `is_template` and `variant_of` in the InvenTree part model? I'm trying to understand the data model before writing tests",
    "should_trigger": false
  },
  {
    "query": "look at test-cases/api/parts/ and tell me which TC IDs are listed in index.md but don't have corresponding .md files yet",
    "should_trigger": false
  },
  {
    "query": "Create 10 manual UI test cases for the \"Units of measure configuration\"",
    "should_trigger": false
  },
  {
    "query": "Cover with tests the main CRUD operation for the Stocks module in the my app",
    "should_trigger": true
  },
  {
    "query": "Cover with manual tests the UI functionality for Part revisions — creation, constraints (circular references, unique codes, template restrictions)",
    "should_trigger": false
  },
  {
    "query": "Create API manual tests for CRUD operations on Parts and Part Categories",
    "should_trigger": true
  },
  {
    "query": "Cover Edge cases for api manual tests (invalid payloads, unauthorized access, conflict scenarios)",
    "should_trigger": true
  }
]